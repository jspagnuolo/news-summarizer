# OpenAI API Key (for summarization)
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-key-here

# GitHub Personal Access Token (for commits)
# Create at: https://github.com/settings/tokens
# Required scopes: repo (full control)
GITHUB_TOKEN=ghp_your-token-here

# Cloudflare Zone ID (for cache purging)
# Find in: Cloudflare Dashboard > Your Domain > Overview (right sidebar)
CLOUDFLARE_ZONE_ID=your-zone-id-here

# Cloudflare API Token (for cache purging)
# Create at: Cloudflare Dashboard > My Profile > API Tokens
# Required permissions: Zone > Cache Purge > Purge
CLOUDFLARE_API_TOKEN=your-api-token-here

# ============================================================================
# IMPORTANT: These are secrets and should NEVER be committed to git
# ============================================================================
#
# For local development: Create a .env file (already gitignored)
# For production: Set these as Cloudflare Worker secrets:
#   npx wrangler secret put OPENAI_API_KEY
#   npx wrangler secret put GITHUB_TOKEN
#   npx wrangler secret put CLOUDFLARE_ZONE_ID
#   npx wrangler secret put CLOUDFLARE_API_TOKEN
#
# Public variables (GITHUB_OWNER, GITHUB_REPO) are set in wrangler.toml
